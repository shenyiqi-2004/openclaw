# Status

This skill is disabled and moved out of the global skills directory.

Reason:
- The `summarize` CLI is not installed on this machine.
- Keeping it globally active would advertise a capability that currently cannot run.

To re-enable:
1. Install the `summarize` CLI.
2. Move this folder back into `C:\Users\w\.codex\skills\`.
