{
  "id": "openclaw-orchestration",
  "uiHints": {
    "maxParallelReadOnly": {
      "label": "Max Parallel Read-Only Tools",
      "help": "Maximum number of read-only tools to run in parallel in auto mode (default: 10)"
    }
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": {
        "type": "boolean"
      },
      "maxParallelReadOnly": {
        "type": "integer",
        "minimum": 1,
        "maximum": 50
      }
    }
  }
}
